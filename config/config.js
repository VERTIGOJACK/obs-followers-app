export const appID = "n5ngz9v4dlmx687gsnivlt99gn517b";

// speedSeconds = How many seconds will a single item be shown on screen
// delaySeconds = How many seconds will the app be inactive before looping through again
// textColor = Color of text in hex, rgb, or alias

export const followersAmount = 25;
export const speedSeconds = 3;
export const delaySeconds = 60;
export const refreshDataAfterMinutes = 5;
export const textColor = "white";
